---
import { Menu } from "@lucide/astro";
import GithubIcon from "./icons/GithubIcon.astro";
import ThemeToggle from "./ThemeToggle.astro";
import { siteNavLinks } from "../lib/nav";
---

<div
  id="app-header"
  class="center group sticky top-0 z-30 w-full border-b border-white/10 bg-primary-950/80 py-3 backdrop-blur-xl transition-[padding] duration-200 ease-out data-scrolled:py-1.5 motion-reduce:transition-none dark:bg-gray-950/75"
>
  <header
    class="app-content items-center justify-between gap-4 px-4 text-white"
  >
    <a href="/" class="flex shrink-0 items-center gap-3">
      <img
        src="/logo.svg"
        alt=""
        width="44"
        height="44"
        class="w-10 transition-[width] duration-200 ease-out group-data-scrolled:w-7 motion-reduce:transition-none sm:w-11"
      />
      <div
        class="flex flex-col gap-0 text-base transition-[font-size] duration-200 ease-out *:leading-none group-data-scrolled:text-sm motion-reduce:transition-none sm:text-lg"
      >
        <span>Causa</span>
        <span class="font-bold">Mortis</span>
      </div>
    </a>

    <div class="flex items-center gap-2 sm:gap-6">
      <nav aria-label="Navegação principal" class="hidden sm:block">
        <ul
          class="flex gap-6 text-base transition-[font-size] duration-200 ease-out group-data-scrolled:text-sm motion-reduce:transition-none"
        >
          {
            siteNavLinks.map((link) => (
              <li>
                <a
                  href={link.href}
                  target={link.external ? "_blank" : undefined}
                  rel={link.external ? "noopener noreferrer" : undefined}
                  class="flex items-center gap-1.5 text-white/85 transition-colors hover:text-white"
                >
                  {link.icon === "github" && (
                    <GithubIcon class="size-4 transition-[width,height] duration-200 ease-out group-data-scrolled:size-3.5 motion-reduce:transition-none" />
                  )}
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <ThemeToggle />

      <details class="relative sm:hidden">
        <summary
          class="flex cursor-pointer list-none items-center rounded-lg p-2 text-white transition-colors select-none hover:bg-white/10 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
          aria-label="Abrir menu de navegação"
        >
          <Menu
            class="size-5 transition-[width,height] duration-200 ease-out group-data-scrolled:size-4 motion-reduce:transition-none"
          />
        </summary>
        <nav aria-label="Menu de navegação" class="chart-export-menu right-0">
          <ul class="flex flex-col gap-1">
            {
              siteNavLinks.map((link) => (
                <li>
                  <a
                    href={link.href}
                    target={link.external ? "_blank" : undefined}
                    rel={link.external ? "noopener noreferrer" : undefined}
                    class="chart-export-item"
                  >
                    {link.icon === "github" && <GithubIcon class="size-4" />}
                    {link.label}
                  </a>
                </li>
              ))
            }
          </ul>
        </nav>
      </details>
    </div>
  </header>
</div>

<div id="app-header-sentinel" aria-hidden="true" class="h-px w-full"></div>

<script>
  const header = document.getElementById("app-header");
  const sentinel = document.getElementById("app-header-sentinel");

  if (header && sentinel) {
    const observer = new IntersectionObserver(
      ([entry]) =>
        header.toggleAttribute("data-scrolled", !entry.isIntersecting),
      { threshold: 0 },
    );
    observer.observe(sentinel);
  }
</script>
